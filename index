def index(lista):
    list = []
    a = 0
    while a < len(lista):
        if  max(lista) == lista[a]:
            list.append(a)
        a += 1
    return list
 
x = [1,1,1,5,5,5,2,2,2,5]
print(index(x))
